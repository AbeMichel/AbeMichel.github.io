document.addEventListener('DOMContentLoaded', function(){
    // create a list of all images in the carousel
    // select 1 as the front image
    // when prev or next is clicked move the current index
    // have the other images rotate positions
    // update z-indexes
});